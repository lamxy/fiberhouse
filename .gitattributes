# Keep repository text files and cross-platform working trees on LF.
* text=auto eol=lf

# Windows command scripts require CRLF for reliable cmd.exe execution.
*.bat text eol=crlf
*.cmd text eol=crlf
